bundle:
  name: smil

targets:
  dev:
    mode: production
    default: true
    workspace:
      host: https://dbc-436077ac-8e41.cloud.databricks.com
      root_path: ~/Workspace/Shared/pipelines/${bundle.name}/${bundle.target}

include:
  - ./resources/pipelines/*.yaml
